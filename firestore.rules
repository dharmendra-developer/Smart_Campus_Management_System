rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This Flask application uses the Firebase Admin SDK on the server.
    // Client-side direct Firestore access is intentionally blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
